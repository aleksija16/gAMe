@page
@model GAMe.Pages.RezervacijaOtkaziModel
@{
}

@if (SessionClass.TipKorisnika == "K")
{
    <div class="obrisi">
        <div id="formContent">
            <form method="post">
                <h5 style="color:white;">Da li ste sigurni da želite da otkazete rezervaciju?</h5>
                <button type="submit" class="btn btn-danger">Da</button>
                <a asp-page="./IgraSve">Ne</a>

            </form>
        </div>
    </div>
}
else
{
    <div class="alert alert-danger" role="alert" style="text-align: center;"><i class="fas fa-exclamation-triangle"></i> Nemate pristup ovoj stranici, samo korisnik moze da otkaze rezervaciju. <i class="fas fa-exclamation-triangle"></i></div>
}